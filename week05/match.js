function match(selector, element) {
  let s0 = selector.split(/\s+/).reverse(); // 待匹配的复合选择器
  let s1 = []; // 已匹配的

  let el = element;
  function selectorMatch(el, selector) {
    // 属性
    
  }
  while (el) {

  }

  return true;
}

match("div #id.class", document.getElementById("id"));